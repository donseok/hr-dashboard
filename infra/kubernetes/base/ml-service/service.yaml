apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: hr-dashboard
  labels:
    app: ml-service
spec:
  selector:
    app: ml-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
